# 数据库连接（默认本地 MySQL，无密码）
# 先运行：mysql -u root < setup-db.sql 创建数据库
DATABASE_URL=mysql://root:@localhost:3306/exam_system

# JWT 密钥
JWT_SECRET=exam_system_jwt_secret_key

# 环境
NODE_ENV=development

# 端口
PORT=3000
