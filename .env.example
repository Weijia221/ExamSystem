# 数据库连接（Railway MySQL 提供）
DATABASE_URL=mysql://root:password@host:port/database

# JWT 密钥（任意随机字符串）
JWT_SECRET=your-random-secret-here

# OAuth（可选，留空则使用本地开发用户）
OAUTH_SERVER_URL=

# 环境
NODE_ENV=production

# 端口（Railway 自动设置）
PORT=3000
